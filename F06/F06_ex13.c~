#include<stdio.h>

void add(int *);

int main(){
  int a[]={0,4999};

  add(&a);
  
  return 0;
}

void add(int a[]){
  int *sub,i;

  sub = (int *)malloc(n*sizeof(int));

  sub = a[0];

  for(i=0;i<n;i++){
    scanf("%d",sub);
    sub++;
  }
  
  sub = a[1];

  a = sub;
  
}
